import { StopPropogationDirective } from './stop-propogation.directive';

describe('StopPropogationDirective', () => {
  it('should create an instance', () => {
    const directive = new StopPropogationDirective();
    expect(directive).toBeTruthy();
  });
});
